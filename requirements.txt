# --- Frameworks ---
django>=4.2.0           # 유저 관리 및 DB 어드민
fastapi>=0.100.0        # 고성능 AI 파이프라인 엔진
uvicorn[standard]       # FastAPI 실행을 위한 ASGI 서버

# --- Database & Models ---
mysqlclient             # MySQL 연결 (Django/FastAPI 공용)
pandas                  # CSV 데이터 가공 및 DB 주입용
pydantic>=2.0.0         # FastAPI 데이터 검증용

# --- AI & LLM Orchestration ---
langchain               # LLM 파이프라인 구축
langchain-openai        # OpenAI 연동 (필요 시)
langgraph               # 복잡한 워크플로우 제어 (선택 사항)

# --- API & Networking ---
httpx                   # FastAPI에서 FDA API를 호출할 비동기 클라이언트
requests                # 일반적인 동기 HTTP 요청용
python-dotenv           # .env 파일에서 API 키/DB 비번 로드용

# --- Utils ---
openpyxl                # 엑셀(.xlsx) 파일 처리용
cryptography            # 보안 및 암호화 관련